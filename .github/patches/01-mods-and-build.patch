diff --git a/disgaea_work/mods/cheat_shop/apply_cheat_shop.cpp b/disgaea_work/mods/cheat_shop/apply_cheat_shop.cpp
new file mode 100644
index 0000000..8e723b8
--- /dev/null
+++ b/disgaea_work/mods/cheat_shop/apply_cheat_shop.cpp
@@ -0,0 +1,41 @@
+#define WIN32_LEAN_AND_MEAN
+#include <windows.h>
+#include <cstdio>
+#include <cstring>
+#include <string>
+
+static bool FileExists(const char* path) {
+    const DWORD attr = GetFileAttributesA(path);
+    return attr != INVALID_FILE_ATTRIBUTES && (attr & FILE_ATTRIBUTE_DIRECTORY) == 0;
+}
+
+static bool EnsureDirectory(const std::string& path) {
+    const DWORD attr = GetFileAttributesA(path.c_str());
+    if (attr != INVALID_FILE_ATTRIBUTES && (attr & FILE_ATTRIBUTE_DIRECTORY) != 0) return true;
+    const size_t slash = path.find_last_of("\\/");
+    if (slash != std::string::npos) {
+        const std::string parent = path.substr(0, slash);
+        if (!parent.empty() && parent.back() != ':') EnsureDirectory(parent);
+    }
+    if (CreateDirectoryA(path.c_str(), nullptr)) return true;
+    return GetLastError() == ERROR_ALREADY_EXISTS;
+}
+
+int main() {
+    char self[MAX_PATH] = {};
+    if (!GetModuleFileNameA(nullptr, self, MAX_PATH)) return 1;
+    char* slash = strrchr(self, '\\');
+    if (!slash) return 1;
+    *slash = '\0';
+
+    const std::string mod_dir = self;
+    const std::string source = mod_dir + "\\cheatSetting.dat";
+    const std::string game_dir = mod_dir + "\\..\\..";
+    const std::string database_dir = game_dir + "\\data\\database";
+    const std::string target = database_dir + "\\cheatSetting.dat";
+
+    if (!FileExists(source.c_str())) return 2;
+    if (!EnsureDirectory(database_dir)) return 3;
+    if (!CopyFileA(source.c_str(), target.c_str(), FALSE)) return 4;
+    return 0;
+}
diff --git a/disgaea_extract/mods/cheat_shop/mod.json b/disgaea_work/mods/cheat_shop/mod.json
index 1094e9e..08fe4cc 100644
--- a/disgaea_extract/mods/cheat_shop/mod.json
+++ b/disgaea_work/mods/cheat_shop/mod.json
@@ -2,39 +2,12 @@
   "id": "cheat_shop_expander",
   "name": "Cheat Shop Ilimitado (+5000% Cap)",
   "category": "Balanceamento & Trapaças",
-  "version": "1.0.0",
+  "version": "1.1.0",
   "author": "Disgaea Mayhem Mod Team",
-  "description": "Expande o limite de pontos e multiplicadores no NPC de Trapaças (Cheat Shop) para até +5.000% de bônus de EXP, Mana, HL e Drops, além de controle avançado de estrelas de dificuldade dos inimigos.",
-  "type": "toggle",
-  "enabled": true,
-  "components": "Tabela de trapaças (cheatSetting.dat) + Limites de pontos",
-  "options": [
-    {
-      "id": "exp_cap",
-      "name": "Multiplicador Máximo de EXP (%)",
-      "type": "slider_int",
-      "min": 100,
-      "max": 5000,
-      "default": 5000,
-      "value": 5000
-    },
-    {
-      "id": "mana_cap",
-      "name": "Multiplicador Máximo de Mana (%)",
-      "type": "slider_int",
-      "min": 100,
-      "max": 5000,
-      "default": 5000,
-      "value": 5000
-    },
-    {
-      "id": "hl_cap",
-      "name": "Multiplicador Máximo de HL Dinheiro (%)",
-      "type": "slider_int",
-      "min": 100,
-      "max": 5000,
-      "default": 5000,
-      "value": 5000
-    }
-  ]
+  "description": "Aplica a tabela modificada do Cheat Shop com limites expandidos para até +5.000% de EXP, Mana, HL e Drops.",
+  "type": "action",
+  "auto_apply": true,
+  "action_label": "Re-Apply",
+  "executable": "APLICAR_MOD_CHEAT_SHOP.exe",
+  "components": "cheatSetting.dat + aplicador nativo auto-contido"
 }
diff --git a/disgaea_work/mods/dark_assembly/apply_dark_assembly.cpp b/disgaea_work/mods/dark_assembly/apply_dark_assembly.cpp
new file mode 100644
index 0000000..1eacf7f
--- /dev/null
+++ b/disgaea_work/mods/dark_assembly/apply_dark_assembly.cpp
@@ -0,0 +1,41 @@
+#define WIN32_LEAN_AND_MEAN
+#include <windows.h>
+#include <cstdio>
+#include <cstring>
+#include <string>
+
+static bool FileExists(const char* path) {
+    const DWORD attr = GetFileAttributesA(path);
+    return attr != INVALID_FILE_ATTRIBUTES && (attr & FILE_ATTRIBUTE_DIRECTORY) == 0;
+}
+
+static bool EnsureDirectory(const std::string& path) {
+    const DWORD attr = GetFileAttributesA(path.c_str());
+    if (attr != INVALID_FILE_ATTRIBUTES && (attr & FILE_ATTRIBUTE_DIRECTORY) != 0) return true;
+    const size_t slash = path.find_last_of("\\/");
+    if (slash != std::string::npos) {
+        const std::string parent = path.substr(0, slash);
+        if (!parent.empty() && parent.back() != ':') EnsureDirectory(parent);
+    }
+    if (CreateDirectoryA(path.c_str(), nullptr)) return true;
+    return GetLastError() == ERROR_ALREADY_EXISTS;
+}
+
+int main() {
+    char self[MAX_PATH] = {};
+    if (!GetModuleFileNameA(nullptr, self, MAX_PATH)) return 1;
+    char* slash = strrchr(self, '\\');
+    if (!slash) return 1;
+    *slash = '\0';
+
+    const std::string mod_dir = self;
+    const std::string source = mod_dir + "\\wish.dat";
+    const std::string game_dir = mod_dir + "\\..\\..";
+    const std::string database_dir = game_dir + "\\data\\database";
+    const std::string target = database_dir + "\\wish.dat";
+
+    if (!FileExists(source.c_str())) return 2;
+    if (!EnsureDirectory(database_dir)) return 3;
+    if (!CopyFileA(source.c_str(), target.c_str(), FALSE)) return 4;
+    return 0;
+}
diff --git a/disgaea_extract/mods/dark_assembly/mod.json b/disgaea_work/mods/dark_assembly/mod.json
index c512070..3710cab 100644
--- a/disgaea_extract/mods/dark_assembly/mod.json
+++ b/disgaea_work/mods/dark_assembly/mod.json
@@ -2,19 +2,12 @@
   "id": "dark_assembly_approval",
   "name": "Dark Assembly - Aprovação 100% dos Senadores",
   "category": "Qualidade de Vida (QoL)",
-  "version": "1.0.0",
+  "version": "1.1.0",
   "author": "Disgaea Mayhem Mod Team",
-  "description": "Aprova instantaneamente todas as propostas, desejos e leis na Assembleia Sombria com 100% de votos favoráveis dos senadores, sem necessidade de suborno com itens ou combate.",
-  "type": "toggle",
-  "enabled": true,
-  "components": "Tabela de desejos (wish.dat) + Override nativo de senadores",
-  "options": [
-    {
-      "id": "instant_pass",
-      "name": "Aprovação Imediata sem Animação de Voto",
-      "type": "toggle",
-      "default": true,
-      "value": true
-    }
-  ]
+  "description": "Aplica a tabela modificada da Dark Assembly para propostas, desejos e leis com aprovação garantida.",
+  "type": "action",
+  "auto_apply": true,
+  "action_label": "Re-Apply",
+  "executable": "APLICAR_MOD_DARK_ASSEMBLY.exe",
+  "components": "wish.dat + aplicador nativo auto-contido"
 }
diff --git a/disgaea_extract/mods/dlc_unlocker/mod.json b/disgaea_work/mods/dlc_unlocker/mod.json
index 5b72996..2dfa4eb 100644
--- a/disgaea_extract/mods/dlc_unlocker/mod.json
+++ b/disgaea_work/mods/dlc_unlocker/mod.json
@@ -6,6 +6,7 @@
   "author": "Disgaea Mayhem Mod Team",
   "description": "Emula ISteamApps e ISteamInventory via SmokeAPI, converte consumíveis para resgate offline infinito no NPC Carlbunch e sincroniza 30x Boost Tickets com multiplicador de +900% na sessão ativa.",
   "type": "action",
+  "auto_apply": true,
   "action_label": "Apply",
   "executable": "APLICAR_MOD_DLC.exe",
   "components": [
diff --git a/disgaea_extract/native/mod_menu_overlay/build.ps1 b/disgaea_work/native/mod_menu_overlay/build.ps1
index be05140..20f8fc1 100644
--- a/disgaea_extract/native/mod_menu_overlay/build.ps1
+++ b/disgaea_work/native/mod_menu_overlay/build.ps1
@@ -8,9 +8,20 @@ $output = Join-Path $outputDirectory 'DisgaeaMayhemModMenu.dll'
 $gcc = 'C:\TDM-GCC-64\bin\gcc.exe'
 $gxx = 'C:\TDM-GCC-64\bin\g++.exe'
 
+# Prefer the project's TDM-GCC toolchain, but allow a MinGW toolchain from PATH
+# so the same build is reproducible on CI and other Windows machines.
+if (-not (Test-Path -LiteralPath $gcc -PathType Leaf)) {
+    $gccCommand = Get-Command gcc.exe -ErrorAction SilentlyContinue
+    if ($gccCommand) { $gcc = $gccCommand.Source }
+}
+if (-not (Test-Path -LiteralPath $gxx -PathType Leaf)) {
+    $gxxCommand = Get-Command g++.exe -ErrorAction SilentlyContinue
+    if ($gxxCommand) { $gxx = $gxxCommand.Source }
+}
+
 foreach ($compiler in @($gcc, $gxx)) {
     if (-not (Test-Path -LiteralPath $compiler -PathType Leaf)) {
-        throw "Compilador obrigatorio ausente: $compiler"
+        throw "Compilador MinGW obrigatorio ausente: $compiler"
     }
 }
 